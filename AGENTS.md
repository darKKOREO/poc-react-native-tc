# Bare React Native CLI project

This is a bare React Native CLI project (Expo has been removed). `react-native` is aliased to `npm:react-native-tvos`. Read the exact versioned docs at https://reactnative.dev/docs before writing any code, and check `react-native-tvos` release notes for TV-specific APIs.
